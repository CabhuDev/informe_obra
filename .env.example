# ────────────────────────────────────────────────
# VARIABLES DE ENTORNO VPS - obratec.app
# ────────────────────────────────────────────────
# IMPORTANTE: Renombra este archivo a .env y configura tus valores

NODE_ENV=production

# Configuración N8N
N8N_PORT=5678
N8N_HOST=tu-dominio.com
N8N_PROTOCOL=https
N8N_EDITOR_BASE_URL=https://n8n.tu-dominio.com
WEBHOOK_URL=https://tu-dominio.com/webhook/form-obra

# Configuración de seguridad N8N - MÁXIMA PROTECCIÓN
N8N_USER_MANAGEMENT_DISABLED=true
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=tu_usuario_admin
N8N_BASIC_AUTH_PASSWORD=tu_password_seguro
N8N_RUNNERS_ENABLED=false
N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
N8N_ENCRYPTION_KEY=tu_clave_32_caracteres_aleatoria

# ────────────────────────────────────────────────
# CLAVES DE TERCEROS (IA, PDF, EMAIL…)
# ────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-tu_clave_openai

# Google APIs (Drive + Gmail OAuth2)
GOOGLE_CLIENT_ID=tu_google_client_id
GOOGLE_CLIENT_SECRET=tu_google_client_secret
GOOGLE_REDIRECT_URI=https://n8n.tu-dominio.com/rest/oauth2-credential/callback

# Gmail OAuth2 (mismo proyecto de Google Cloud)
GMAIL_CLIENT_ID=tu_gmail_client_id
GMAIL_CLIENT_SECRET=tu_gmail_client_secret
GMAIL_USER=tu_email@gmail.com
GMAIL_PASSWORD=tu_app_password

# Destinatarios de email (separados por coma)
RECIPIENT_EMAIL=destinatario@email.com

# ────────────────────────────────────────────────
# CONFIGURACIÓN ADICIONAL VPS
# ────────────────────────────────────────────────

# Base de datos (SQLite por defecto)
DB_TYPE=sqlite
DB_SQLITE_DATABASE=/home/node/.n8n/database.sqlite

# Zona horaria
GENERIC_TIMEZONE=Europe/Madrid
